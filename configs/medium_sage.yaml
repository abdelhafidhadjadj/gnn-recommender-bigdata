data:
  data_dir: data/medium

model:
  emb_dim: 64
  dropout: 0.1
  n_layers: 1
  use_residual: true

train:
  num_epochs: 100
  lr: 0.005
  min_epochs: 40
  patience: 15
  eval_every: 10

checkpoint:
  dir: checkpoints/sage

model_type: sage
seed: 42
